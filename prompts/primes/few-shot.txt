[DESCRIÇÃO DA TAREFA]
Gerar testes unitários em Java (JUnit 5) para o método nextPrime(int n) da classe org.apache.commons.math3.primes.Primes.

[RESTRIÇÃO DE ESCOPO]
Gerar casos de teste apenas para o método nextPrime(int n).
Não criar testes para outros métodos da classe.

[CONFORMIDADE DOS NOMES DE TESTE]
Todos os casos de teste devem ter nomes que façam referência explícita ao método nextPrime,
como por exemplo: testNextPrime_*, nunca utilizando nomes de outros métodos ou termos não relacionados.

[EXEMPLOS DE CASOS DE TESTE (APENAS MÉTODOS)]
Use os exemplos abaixo APENAS como referência de estilo, organização e formato de métodos de teste.
Não reutilize valores, lógica ou comportamento destes exemplos.

======== EXEMPLO 1 ========
@Test
void testAddPositiveNumbers() {
    int result = Calculator.add(5, 3);
    assertEquals(8, result);
}

======== EXEMPLO 2 ========
@Test
void testReverseSimpleString() {
    String reversed = StringUtils.reverse("abc");
    assertEquals("cba", reversed);
}

======== EXEMPLO 3 ========
@Test
void testDivisionByZeroThrows() {
    assertThrows(ArithmeticException.class, () -> Calculator.divide(10, 0));
}

[INSTRUÇÕES]
Com base APENAS no estilo demonstrado pelos exemplos acima, gere uma classe de teste JUnit 5 completa para o método nextPrime(int n).

[FORMATO DE SAÍDA]
Forneça apenas o código Java completo e compilável da classe de teste, sem comentários ou explicações adicionais.

