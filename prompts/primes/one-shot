[DESCRIÇÃO DA TAREFA]
Gerar testes unitários em Java (JUnit 5) para o método nextPrime(int n)
da classe org.apache.commons.math3.primes.Primes.

[RESTRIÇÃO DE ESCOPO]
Gerar casos de teste apenas para o método nextPrime(int n).
Não criar testes para outros métodos da classe.

[CONFORMIDADE DOS NOMES DE TESTE]
Todos os casos de teste devem ter nomes que façam referência explícita
ao método nextPrime, como por exemplo: testNextPrime_*.

[EXEMPLO DE CLASSE DE TESTE (ONE-SHOT)]
Use o exemplo abaixo exclusivamente como referência de estrutura,
organização, imports e formato de uma classe de teste JUnit 5 válida.
Não reutilize os valores de entrada, resultados esperados ou a lógica
do teste apresentado no exemplo.

======== EXEMPLO ========
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.apache.commons.math3.primes.Primes;
import org.junit.jupiter.api.Test;

public class PrimesOneShotExampleTest {

    @Test
    void testNextPrimeExample() {
        int result = Primes.nextPrime(10);
        assertEquals(11, result);
    }
}

======== FIM DO EXEMPLO ========

[INSTRUÇÕES]
Com base no código fornecido da classe Primes e seguindo a estrutura
demonstrada no exemplo acima, gere uma classe de teste JUnit 5 completa
para o método nextPrime(int n), incluindo os imports necessários.

[FORMATO DE SAÍDA]
Forneça apenas o código Java completo e compilável da classe de teste,
sem comentários ou explicações adicionais.
