[1] Tarefa
Gerar testes unitários em Java (JUnit 5) para o método pop()
da classe com.thealgorithms.datastructures.stacks.StackArray.

[2] Restrição de Escopo
Gerar casos de teste apenas para o método pop().
Não criar testes para outros métodos da classe.

[3] Exemplos de Classe de Teste (Few-Shot)

Use o exemplo abaixo apenas como referência de estrutura, organização, imports e formato de uma classe de teste JUnit 5 com múltiplos casos de teste.

Não reutilize valores de entrada, resultados esperados ou a lógica dos testes apresentados.

// Exemplos

import static org.junit.jupiter.api.Assertions.assertEquals;

import com.thealgorithms.datastructures.stacks.StackArray;
import org.junit.jupiter.api.Test;

public class StackArrayFewShotExampleTest {

    @Test
    void testPopWithMultipleElements() {
        StackArray stack = new StackArray(3);
        stack.push(1);
        stack.push(2);
        stack.push(3);
        assertEquals(3, stack.pop());
    }

    @Test
    void testPopWithSingleElement() {
        StackArray stack = new StackArray(2);
        stack.push(7);
        assertEquals(7, stack.pop());
    }

    @Test
    void testPopAfterIntermediateOperations() {
        StackArray stack = new StackArray(3);
        stack.push(4);
        stack.push(6);
        stack.pop();
        stack.push(9);
        assertEquals(9, stack.pop());
    }
}

// Fim dos Exemplos

[4] Geração do Código Final (JUnit 5)
Forneça apenas o código Java completo e compilável da classe de teste.

[5] Requisitos
- Não incluir comentários, explicações ou texto fora do código Java;
- Nomes dos testes devem seguir o padrão: testPop_*.

